# Subject Groups Endpoints — /v1/academics/subject-groups
Manage subject groupings (Science, Commerce, Arts). Permission: `academics.subject_group.manage` for write, `academics.subject.read` for read.
