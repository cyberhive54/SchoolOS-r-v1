# Run 005 — 17/03/2026 13:10

## Objective
Fix all remaining code-review blockers on Task #1 (Academics Module).

## Issues Fixed

### 1. Promotion Processor — EventEmitter emission
- Updated `promotion.processor.ts` to inject `EventEmitter2` and emit `student.promoted` event per student after each successful processing step.

### 2. Teacher Assignments — Real Staff Dropdown
- Added `GET /users/school-members?role=teacher` endpoint in `UsersModule` (new `users.controller.ts` + `users.module.ts` updated).
- Rewrote `assignments/page.tsx` with a real `TeacherCell` component using a dropdown to select from actual school staff.
- Assignment is optimistically tracked in state; remove also supported via × button.
- Added `GET /academics/class-sections/subject-teachers` bulk endpoint for loading existing assignments.

### 3. Classes Page — Two-panel layout
- Rewrote `classes/page.tsx` to show a left panel (classes with reorder via ▲▼ and CRUD) and right panel (sections for selected class, with inline add/edit/delete).

### 4. Class-Sections Page — Inline class-teacher assignment
- Added `ClassTeacherCell` component with staff dropdown for assigning/removing class teachers per row.
- Loads staff from `/users/school-members?role=teacher`.

### 5. Subject-Groups — Drag/add-remove subject mapping UX
- Rewrote `subject-groups/page.tsx`: expanded panel shows subject pills with × remove button + add-subject dropdown.
- Unassigned subjects only shown in the add dropdown (already-assigned ones filtered out).

### 6. `class_sections.status` field + DB constraint
- Added `status` VARCHAR(20) DEFAULT 'active' to `ClassSectionEntity`.
- Created migration `005-class-sections-status.ts` with:
  - `ALTER TABLE class_sections ADD COLUMN status`
  - `CREATE INDEX idx_class_sections_school_status ON class_sections(school_id, status)`
  - `CREATE UNIQUE INDEX udx_academic_years_school_current ON academic_years(school_id) WHERE is_current = TRUE`

### 7. Promotion Tests — New DTO + queue mock
- Rewrote `service.spec.ts` to use the new `promotions[]` array DTO, `getQueueToken` mock, and four test cases covering: NotFoundException (source year), NotFoundException (target year), ConflictException (duplicate key), successful queue return.

### 8. `removeSubjectTeacher` — Subject-ID based removal
- Changed `removeSubjectTeacher` to look up by `subject_id` (not internal `id`) for cleaner frontend API.
- Added `listAllSubjectTeachers(schoolId, { academic_year_id })` service method + `GET /academics/class-sections/subject-teachers` controller endpoint.

## Test Results
- **40/40 tests passing** across 11 suites (up from 36 before promotion test fix).
- TypeScript build: clean (0 errors).
- Migration 005 applied successfully on startup.

## Status
Task #1 blockers fully resolved. Ready for code review pass 3.
