# AI Run Log — run-004
**Date:** 17/03/2026  
**Time:** 13:00  
**Scope:** Code review fixes for Task #1 (Academics Module)

## Changes Made

### Backend — Promotions endpoint (fully spec-compliant)
- **Route fixed**: `POST /academics/promotions/bulk-promote` → `POST /academics/promotions`
- **Request body fixed**: now takes `promotions[]` array with per-student entries `{ student_id, from_class_section_id, to_class_section_id, status }`
- **Status enum added**: `promoted | detained | transferred_out`
- **BullMQ job**: returns `202 Accepted` with `job_id`, `status: 'queued'`, `total`
- **Idempotency key**: required `Idempotency-Key` header; returns 409 if duplicate job exists
- **PromotionProcessor**: new `WorkerHost` processes job, logs per-item results
- **AcademicsModule**: registers `PROMOTION_QUEUE` via `BullModule.registerQueue()`
- **route.md**: updated with full API contract

### Backend — Permissions
- Added `ACADEMICS_CLASS_MANAGE: 'academics.class.manage'`
- Added `ACADEMICS_SUBJECT_MANAGE: 'academics.subject.manage'`
- Added both to `admin` role defaults in `ROLE_DEFAULT_PERMISSIONS`
- Classes controller: all 5 endpoints now use `ACADEMICS_CLASS_MANAGE`
- Subjects controller: all 5 endpoints now use `ACADEMICS_SUBJECT_MANAGE`

### Backend — Type-safety fix (subject-groups/service.ts)
- Removed `as never` casts in `findAll()` and `toDto()`
- Added explicit `SubjectDto` type import
- Type guard `filter((s): s is SubjectDto => s !== null)` replaces unsafe cast
- `toDto()` signature changed from `subjects: unknown[]` → `subjects: SubjectDto[]`

### Backend — Seed data (spec-aligned)
- 3 classes only: Grade 1, Grade 2, Grade 3
- 2 sections: A, B
- 5 core subjects: English, Mathematics, Science, Social Studies, Hindi
- 6 class-sections (3×2) wired with academic year 2025-26
- All 5 subjects assigned to each class-section (30 assignments)
- Imports: added `ClassSectionEntity`, `ClassSectionSubjectEntity`

### Build fix — tsconfig.build.json
- Added `"incremental": false` to prevent tsc from skipping middleware files when dist/ is deleted
- Root cause: `tsconfig.json` has `incremental: true`; deleting dist without deleting `.tsbuildinfo` caused tsc to emit nothing

### Frontend — 2 missing pages created
- `/dashboard/academics/assignments` — matrix view, year/section dropdown, teacher assignment per cell, skeleton loaders, toast feedback, manage-role permission guard
- `/dashboard/academics/promotion` — 4-step wizard (select years → select class-section → review students → done), 202 Accepted job_id displayed, polled, toast on completion

### Frontend — Permission guards
- Both new pages check `user.role` against `['super_admin', 'admin']` and redirect to `/academics` if not authorized

### Frontend — Sidebar
- Academics sub-links now expand when on any `/academics/*` route
- 8 sub-links shown: Years, Classes, Sections, Class-Sections, Subjects, Subject Groups, Assignments, Promotion
- All sub-links are `manageOnly: true` (only shown to super_admin/admin)

### Frontend — Academics hub page
- Added 2 new cards: Teacher Assignments and Student Promotion

## Verification
- TypeScript: 0 compile errors
- Backend startup: `AcademicsModule dependencies initialized` ✓
- All 29 routes confirmed mapped including `POST /v1/academics/promotions`
- Frontend: Next.js `Ready in 1405ms` ✓
